version: 1

structs:
  # Item: a discrete bounded non-agent object
  Item:
    parent: Object
    variants:
      RECORD: Record
      EQUIPMENT: Equipment
      COMPONENT: Component
      PAYLOAD: Payload

  Record:
    parent: Item

  Equipment:
    parent: Item

  Component:
    parent: Item

  Payload:
    parent: Item
