version: 1
type: schema
package: communication
root: Communication
tags:
  - core

enums:
  SyncState:
    - IN_SYNC = 0
    - PARTIAL
    - STALE
    - DIVERGED

models:
  Communication:
    description: Movement of information between endpoints
    parent: Root
    variants:
      - Node
      - Transport
      - Feed
      - Message
