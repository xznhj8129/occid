version: 1
type: schema
package: communication
root: Communication
tags:
  - core

enums:
  SyncState:
    - IN_SYNC = 0
    - PARTIAL
    - STALE
    - DIVERGED

  AddressingMode:
    - UNICAST
    - MULTICAST
    - BROADCAST

  ExchangePattern:
    - PUSH
    - REQUEST_RESPONSE

models:
  Communication:
    description: Movement of information between endpoints
    semantic_role: ontology
    parent: Root
    variants:
      - Node
      - Network
      - Link
      - Protocol
      - Message
      - Interface
