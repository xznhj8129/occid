version: 1
type: schema
package: c3
root: CommandMessage
tags:
  - core

models:
  CommandMessage:
    description: Message whose payload directs action
    semantic_role: ontology
    parent: Message
    fields:
      command: Command

  HumanTextMessage:
    semantic_role: specialization
    parent: Message
    fields:
      message_id: optional StringID
      conversation_id: optional StringID
      reply_to_id: optional StringID
      sender_id: optional StringID
      sender_name: optional string
      destination_id: optional StringID
      destination_group: optional string
      kind: optional string
      message: string
      position: optional GlobalPosition
      targets: list[MessageTarget]
