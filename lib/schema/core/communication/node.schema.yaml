# The actual communications endpoint equipment of an entity
version: 1
type: schema
package: node
root: Node
tags:
  - core

enums:
  CapabilityRole:
    - CONTROLLER = 1 << 0
    - RELAY 
    - SENSOR 
    - EFFECTOR 
    - GATEWAY 
    - RECORDER 

models:
  Node:
    description: Communication identity of an entity, including addresses and available links
    semantic_role: ontology
    parent: Communication
    fields:
      node_id: StringID
      entity_id: optional StringID
      roles: list[CapabilityRole]
      addresses: list[NetworkAddress]
      links: map[string, Link]
      radios: map[string, RadioProfile]
      protocols: map[string, Protocol]
