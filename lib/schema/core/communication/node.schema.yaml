version: 1
type: schema
package: node
root: Node
tags:
  - core

enums:
  CapabilityRole:
    - CONTROLLER = 0
    - RELAY
    - SENSOR
    - EFFECTOR
    - GATEWAY
    - RECORDER

models:
  Node:
    description: Endpoint that sends, receives, relays, records, controls, or bridges messages
    parent: Communication

  NodeRef:
    parent: Node
    fields:
      node_id: string
