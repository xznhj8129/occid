version: 1
type: schema
package: interface
root: Interface
tags:
  - core

models:
  Interface:
    description: Control surface, lease, operator input, remote-control mapping, or executor-facing command interface
    parent: Control

  ControlLease:
    parent: Interface
    fields:
      asset_id: string
      holder_id: string
      control_level: ControlLevel
      lease_start: float
      lease_end: float
      lease_rev: int = 0
