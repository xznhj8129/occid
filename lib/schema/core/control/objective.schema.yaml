version: 1
type: schema
package: objective
root: Objective
tags:
  - core

models:
  Objective:
    description: Desired end state with intent, success rule, target, priority, and deadline
    parent: Control
