version: 1
type: schema
package: control
root: Control
tags:
  - core

models:
  Control:
    description: "Scale-invariant; the binding of agents to objectives through structured decomposition"
    parent: Root
    variants:
      - Reasoning
      - Directive
      - Execution
      - Reference
      - Constraint
      - Interface
