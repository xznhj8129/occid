version: 1
type: schema
package: cue
root: Cue
tags:
  - core

models:
  Cue:
    description: Directional cue toward a target or point of interest, distinct from vehicle guidance, navigation, and control state
    parent: State
    fields:
      source_id: optional StringID
      target_id: optional StringID
      bearing_rad: float
      elevation_rad: optional float
      distance_m: optional float
      label: optional string
