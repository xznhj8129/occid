version: 1
type: schema
package: internal
root: Internal
tags:
  - core

models:
  Internal:
    description: "Diagnostic internals of a machine or system (CPU, memory, cycles, firmware, connectivity)"
    parent: State

  FirmwareInfo:
    parent: Internal
    fields:
      name: string
      version: Version
      build: optional string

  RuntimeLoadState:
    parent: Internal
    fields:
      cpu_load: optional int
      cycle_time_us: optional int
