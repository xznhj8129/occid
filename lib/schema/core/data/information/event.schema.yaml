version: 1
type: schema
package: event
root: Event
tags:
  - core

models:
  Event:
    description: "A discrete occurrence — something that happened at a point in time, whether planned or not"
    parent: Information
    variants:
      - FlightEvent
      - MissionEvent

  FlightEvent:
    parent: Event

  MissionEvent:
    parent: Event
