version: 1
type: schema
package: intel
root: Intel
tags:
  - core

models:
  Intel:
    description: "Effect-side data that something happened or changed external to the sender object"
    parent: Information
    variants:
      - Detection
      - Classification
      - Track
      - Assessment

  Classification:
    parent: Intel

  Track:
    parent: Intel

  Assessment:
    parent: Intel
